public class SimpleTrueFalse {
    public boolean solution(boolean x1, boolean x2, boolean x3, boolean x4) {
        boolean answer;
        answer = (x1 || x2) && (x3 || x4);

        return answer;
    }
}

// :: 간단한논리연산
// :: 걸린시간 4분